1. ACID یعنی چی؟
ACID یک کلمه‌ی اختصاریه که ویژگی‌های مهم یک تراکنش خوب در دیتابیس رو خلاصه می‌کنه:

Atomicity (اتمی بودن): یا همه‌ی تغییرات یک تراکنش انجام میشه یا هیچکدوم. نصفه نیمه تغییر نمی‌کنیم.

Consistency (سازگاری): داده‌ها بعد از تراکنش باید همچنان با قوانین اپلیکیشن هماهنگ باشن (مثل اینکه موجودی حساب منفی نشه).

Isolation (ایزوله بودن): تراکنش‌های همزمان نباید تو کار هم دخالت کنن یا خرابش کنن.

Durability (دوام): وقتی دیتابیس گفت «تراکنش موفق بود»، باید مطمئن باشی داده‌ها حتی با قطع برق یا خرابی، حفظ میشن.

✏️ نکته مهم:
نویسنده میگه امروزه خیلی از سیستم‌ها فقط ادعا می‌کنن "ACID" هستن، ولی واقعاً هر کدوم یه مدل خاص پیاده‌سازی کردن. پس باید حواسمون باشه که فقط با شنیدن کلمه‌ی ACID نباید زود باور کنیم!

2. Atomicity یعنی چی؟
اتمی بودن یعنی یا تراکنش کامل انجام میشه یا اصلاً انگار هیچ کاری نکردی.
مثال:
فرض کن میخوای پولی از حساب A به حساب B منتقل کنی. اگه وسط کار برق بره و فقط پول از حساب A کم شده باشه ولی به حساب B نرسیده باشه، یه فاجعه میشه.
با Atomicity، سیستم تضمین می‌کنه که یا هر دو عملیات انجام میشه، یا هیچکدوم.

این Atomicity ربطی به همزمانی چند کاربر نداره (اون مال Isolation هست).

3. Consistency یعنی چی؟
Consistency تو ACID یعنی «داده‌های دیتابیس باید همیشه قوانین منطقی اپلیکیشن رو رعایت کنن».
مثلاً:

مجموع موجودی کل حساب‌های بانکی باید همیشه صفر بشه.

هیچ کاربری نباید ایمیلی تکراری داشته باشه.

✏️ نکته:
دیتابیس خودش همه چیز رو چک نمی‌کنه. برنامه‌نویس باید قوانین درست رو تعریف کنه. بعضی چیزها مثل کلید خارجی (Foreign Key) یا یکتا بودن (Unique Constraint) رو دیتابیس چک می‌کنه، ولی بقیه‌اش به اپلیکیشن بستگی داره.

4. Isolation یعنی چی؟
Isolation یعنی تراکنش‌ها انگار هر کدوم تنها در دیتابیس دارن کار می‌کنن، حتی اگه در واقع همزمان باشن.
اگه دوتا کاربر همزمان یه متغیر رو تغییر بدن، نباید باعث خرابکاری بشه.

مثال:
دوتا نفر همزمان میخوان یه شمارنده رو ۱ عدد افزایش بدن. اگه درست مدیریت نشه، شمارنده فقط ۱بار افزایش پیدا می‌کنه، نه ۲بار.

✏️ نکته:
ایزوله کامل (Serializability) گرون و کند میشه، پس خیلی از دیتابیس‌ها مثل Oracle به جاش از چیز ساده‌تری مثل Snapshot Isolation استفاده می‌کنن.

5. Durability یعنی چی؟
Durability یعنی اگه دیتابیس گفت تراکنش موفق شد، دیگه خیال راحت: داده‌ها باقی می‌مونن، حتی اگه برق بره یا سیستم خراب بشه.

در گذشته دوام یعنی "نوشتن روی نوار مغناطیسی" بود!

الان یعنی "نوشتن روی دیسک یا SSD" یا "کپی گرفتن روی چند سرور (Replication)".

✏️ نکته:
هیچ سیستمی ۱۰۰٪ تضمین‌شده نیست. مثلاً:

برق قطع شه ممکنه داده‌های درحال پردازش از بین برن.

SSDها بدون برق ممکنه تو چند هفته داده از دست بدن.

حتی دستوراتی مثل fsync() هم همیشه امن نیستن چون سخت‌افزارها یا فایل‌سیستم‌ها ممکنه باگ داشته باشن.

پس همیشه باید چند لایه محافظت داشته باشیم: دیسک + رپلیکیشن + بکاپ.